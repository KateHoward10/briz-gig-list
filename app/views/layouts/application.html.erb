<!DOCTYPE html>
<html>
  <head>
    <title>BrizGigList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if current_user.present? %>
      <nav class="px-8 py-4 flex justify-between bg-blue-400">
        <%= link_to "Feed", feed_path, class: "rounded px-2 hover:bg-blue-300" %>
        <%= link_to "Gigs", gigs_path, class: "rounded px-2 hover:bg-blue-300" %>
        <%= link_to "Profile", profile_path, class: "rounded px-2 hover:bg-blue-300" %>
        <%= link_to "Log out", logout_path, class: "rounded px-2 hover:bg-blue-300" %>
      </nav>
    <% end %>
    <main class="container mx-auto mt-16 px-5">
      <%= yield %>
    </main>
  </body>
</html>
