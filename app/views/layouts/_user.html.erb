<%= link_to user.id == current_user.id ? profile_path : "/profiles/#{user.id}", class: "flex items-center" do %>
  <%= image_tag user.image, class: "rounded-full h-6 w-6 mr-1" %>
  <%= user.id == current_user.id ? "You" : user.name %>
<% end %>
