<% if current_user.present? %>
  <div class="flex flex-col items-center my-12">
    <%= image_tag current_user.image, class: "rounded-full h-20 w-20" %>
    <h1><%= current_user.name %></h1>
    <p><%= current_user.email %></p>
  </div>
<% end %>

<% if @interested.any? %>
  <h2 class="text-xl font-bold mb-4">Interested</h2>
  <ul>
    <% @interested.each do |response| %>
      <%= render 'gig', gig: response.gig %>
    <% end %>
  </ul>
<% end %>
<%= paginate(@interested) %>

<% if @going.any? %>
  <h2 class="text-xl font-bold mt-8 mb-4">Going</h2>
  <ul>
    <% @going.each do |response| %>
      <%= render 'gig', gig: response.gig %>
    <% end %>
  </ul>
<% end %>
<%= paginate(@going) %>
