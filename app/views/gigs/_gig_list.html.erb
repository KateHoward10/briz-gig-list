<% if gigs_by_month.any? %>
  <% gigs_by_month.each do |month, gigs| %>
    <%= render 'layouts/date_divider', title: month %>
    <div class="mb-10">
      <% gigs.each do |gig| %>
        <%= render 'gig_list_item', gig: gig, going: gig.responses.where(status: "Going"), interested: gig.responses.where(status: "Interested") %>
      <% end %>
    </div>
  <% end %>
<% else %>
  <p class="text-red-800">No gigs found</p>
<% end %>
