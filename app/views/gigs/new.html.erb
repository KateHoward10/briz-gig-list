<%= render 'shared/back_button', label: @back.present? ? "Back" : "Gigs", link: @back.present? ? @back : gigs_path %>

<h1 class="mb-6 text-xl font-bold">Add a new gig</h1>

<%= render 'form', gig: @gig, venues: @venues %>
