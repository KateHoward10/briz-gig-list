<div class="py-1 px-2 rounded rounded-<%= is_own ? 'br' : 'bl' %>-none bg-gray-300 text-xs italic truncate max-w-full">
  <span class="font-semibold mr-1"><%= is_own ? "You" : post.user.name %></span> — <%= post.text %>
</div>
